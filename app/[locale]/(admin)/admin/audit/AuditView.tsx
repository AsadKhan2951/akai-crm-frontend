"use client";

import { useTranslations } from "next-intl";
import { Button } from "@/components/ui/button";
import { EmptyState, PageHeader } from "@/components/ui-kit";

type Row = Record<string, unknown>;
export function AuditView({ rows, search }: { rows: Row[]; search: string }) { const t = useTranslations("admin"); return <div className="space-y-6"><PageHeader title={t("auditTitle")} description={t("auditDescription")} /><form method="get" className="flex gap-2"><label className="flex-1 text-sm font-medium text-primary">{t("search")}<input name="search" defaultValue={search} className="mt-1 min-h-11 w-full rounded-md border border-slate-300 px-3" /></label><Button type="submit" className="mt-6 min-h-11">{t("search")}</Button></form>{rows.length ? <div className="space-y-3">{rows.map((row, index) => { const changes = (row.changes_json as Record<string, unknown> | null) ?? {}; return <article key={String(row.id ?? index)} className="rounded-lg border border-slate-200 bg-white p-4"><div className="flex flex-col gap-1 md:flex-row md:justify-between"><p className="font-semibold text-primary">{String(row.action)} · {String(row.entity_type)} · <bdi>{String(row.entity_id)}</bdi></p><p className="text-sm text-muted-foreground"><bdi>{String(row.created_at)}</bdi></p></div><div className="mt-3 grid gap-3 md:grid-cols-2"><div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold text-primary">{t("before")}</p><pre className="mt-1 whitespace-pre-wrap text-xs text-muted-foreground">{JSON.stringify(changes.before ?? {}, null, 2)}</pre></div><div className="rounded-md bg-slate-50 p-3"><p className="text-xs font-semibold text-primary">{t("after")}</p><pre className="mt-1 whitespace-pre-wrap text-xs text-muted-foreground">{JSON.stringify(changes.after ?? changes, null, 2)}</pre></div></div></article>; })}</div> : <EmptyState title={t("noDashboardData")} description={t("noDashboardDataHint")} />}</div>; }
